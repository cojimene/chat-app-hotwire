<%= turbo_frame_tag 'rooms_controller' do %>
  <main class="flex w-full h-screen bg-[#181D31]">
    <%= render 'shared/side_bar' %>

    <section class="flex flex-col items-center justify-center gap-5 h-screen text-white w-full">
      <%= image_tag 'chat-icon', width: 200 %>
      <h1 class="font-bold text-4xl my-4">Chat App - Ruby on Rails 7</h1>
      <p class="font-semibold">This is the chat app with RoR 7</p>
    </section>
  </main>
<% end %>