<%= turbo_frame_tag dom_id(room) do %>
  <div class="text-white">
    <%= link_to room_path(room), class: 'font-semibold', data: {turbo_frame: 'rooms_controller'} do %>
      <%= room.name %>
    <% end %>
    -
    <%= link_to 'Edit', edit_room_path(room), class: 'text-sm' %>
  </div>
<% end %>
